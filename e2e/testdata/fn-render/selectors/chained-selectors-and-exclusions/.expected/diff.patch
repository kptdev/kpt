diff --git a/Kptfile b/Kptfile
index 31e16a0..f31c2a8 100644
--- a/Kptfile
+++ b/Kptfile
@@ -18,3 +18,22 @@ pipeline:
         - kind: Service
       exclude:
         - name: legacy-svc
+status:
+  conditions:
+    - type: Rendered
+      status: "True"
+      reason: RenderSuccess
+  renderStatus:
+    mutationSteps:
+      - image: ghcr.io/kptdev/krm-functions-catalog/set-namespace:latest
+        exitCode: 0
+        results:
+          - message: namespace [default] updated to "prod", 1 value(s) changed
+            severity: info
+          - message: all `depends-on` annotations are up-to-date. no `namespace` changed
+            severity: info
+      - image: ghcr.io/kptdev/krm-functions-catalog/set-labels:latest
+        exitCode: 0
+        results:
+          - message: set 2 labels in total
+            severity: info
diff --git a/resources.yaml b/resources.yaml
index 50ecfb5..6ab4b21 100644
--- a/resources.yaml
+++ b/resources.yaml
@@ -2,6 +2,7 @@ apiVersion: apps/v1
 kind: Deployment
 metadata:
   name: web
+  namespace: prod
 spec:
   replicas: 1
 ---
@@ -16,9 +17,11 @@ apiVersion: v1
 kind: Service
 metadata:
   name: web-svc
+  labels:
+    app: myapp
 spec:
   selector:
-    app: web
+    app: myapp
 ---
 apiVersion: v1
 kind: Service
