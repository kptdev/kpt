apiVersion: kpt.dev/v1
kind: Kptfile
metadata:
  name: db
pipeline:
  validators:
    - image: ghcr.io/kptdev/krm-functions-catalog/starlark:v0.5.1 # validates httpbin deployment exists
      configPath: starlark-httpbin-val.yaml
      selectors:
        - kind: Deployment
