apiVersion: kpt.dev/v1
kind: Kptfile
metadata:
  name: app-not-met
pipeline:
  mutators:
    - image: ghcr.io/kptdev/krm-functions-catalog/no-op
      when: resources.exists(r, r.kind == 'ConfigMap' && r.metadata.name == 'unmatched-config')
