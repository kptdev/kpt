diff --git a/Kptfile b/Kptfile
index 21d9773..66b7fc9 100644
--- a/Kptfile
+++ b/Kptfile
@@ -2,6 +2,8 @@ apiVersion: kpt.dev/v1
 kind: Kptfile
 metadata:
   name: wordpress
+  annotations:
+    abc: def
 pipeline:
   mutators:
     - image: ghcr.io/kptdev/krm-functions-catalog/set-annotations:latest
diff --git a/mysql/Kptfile b/mysql/Kptfile
index 3d51a77..965bc63 100644
--- a/mysql/Kptfile
+++ b/mysql/Kptfile
@@ -2,6 +2,9 @@ apiVersion: kpt.dev/v1
 kind: Kptfile
 metadata:
   name: mysql
+  annotations:
+    foo: bar
+    abc: def
 pipeline:
   mutators:
     - image: ghcr.io/kptdev/krm-functions-catalog/set-annotations:latest
diff --git a/mysql/mysql-deployment.yaml b/mysql/mysql-deployment.yaml
index 1b0fbd4..349681e 100644
--- a/mysql/mysql-deployment.yaml
+++ b/mysql/mysql-deployment.yaml
@@ -18,6 +18,8 @@ metadata:
   annotations:
     projectId: 'PROJECT_ID' # kpt-set: ${gcloud.core.project}
     teamname: 'YOURTEAM' # kpt-set: ${teamname}
+    foo: bar
+    abc: def
 spec:
   selector:
     app: wordpress
@@ -33,6 +35,8 @@ metadata:
   annotations:
     projectId: 'PROJECT_ID' # kpt-set: ${gcloud.core.project}
     teamname: 'YOURTEAM' # kpt-set: ${teamname}
+    foo: bar
+    abc: def
 spec:
   resources:
     requests:
@@ -47,6 +51,8 @@ metadata:
   annotations:
     projectId: 'PROJECT_ID' # kpt-set: ${gcloud.core.project}
     teamname: 'YOURTEAM' # kpt-set: ${teamname}
+    foo: bar
+    abc: def
 spec:
   selector:
     matchLabels:
@@ -57,6 +63,9 @@ spec:
       labels:
         app: wordpress
         tier: mysql
+      annotations:
+        foo: bar
+        abc: def
     spec:
       containers:
         - name: mysql
diff --git a/wordpress-deployment.yaml b/wordpress-deployment.yaml
index d018dfa..af25a1f 100644
--- a/wordpress-deployment.yaml
+++ b/wordpress-deployment.yaml
@@ -16,6 +16,7 @@ metadata:
   annotations:
     projectId: 'PROJECT_ID' # kpt-set: ${gcloud.core.project}
     teamname: 'YOURTEAM' # kpt-set: ${teamname}
+    abc: def
 spec:
   type: LoadBalancer
   selector:
@@ -31,6 +32,7 @@ metadata:
   annotations:
     projectId: 'PROJECT_ID' # kpt-set: ${gcloud.core.project}
     teamname: 'YOURTEAM' # kpt-set: ${teamname}
+    abc: def
 spec:
   resources:
     requests:
@@ -45,6 +47,7 @@ metadata:
   annotations:
     projectId: 'PROJECT_ID' # kpt-set: ${gcloud.core.project}
     teamname: 'YOURTEAM' # kpt-set: ${teamname}
+    abc: def
 spec:
   selector:
     matchLabels:
@@ -55,6 +58,8 @@ spec:
       labels:
         app: wordpress
         tier: frontend
+      annotations:
+        abc: def
     spec:
       containers:
         - name: wordpress
