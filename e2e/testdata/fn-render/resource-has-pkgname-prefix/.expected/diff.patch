diff --git a/mysql/mysql-deployment.yaml b/mysql/mysql-deployment.yaml
index c9253c0..d7f1566 100644
--- a/mysql/mysql-deployment.yaml
+++ b/mysql/mysql-deployment.yaml
@@ -5,8 +5,10 @@ metadata:
   labels:
     app: wordpress
   annotations:
-    projectId: 'PROJECT_ID' # kpt-set: ${gcloud.core.project}
-    teamname: 'YOURTEAM' # kpt-set: ${teamname}
+    abc: def
+    foo: bar
+    projectId: PROJECT_ID # kpt-set: ${gcloud.core.project}
+    teamname: YOURTEAM # kpt-set: ${teamname}
 spec:
   selector:
     app: wordpress
@@ -22,8 +24,10 @@ metadata:
   labels:
     app: wordpress
   annotations:
-    projectId: 'PROJECT_ID' # kpt-set: ${gcloud.core.project}
-    teamname: 'YOURTEAM' # kpt-set: ${teamname}
+    abc: def
+    foo: bar
+    projectId: PROJECT_ID # kpt-set: ${gcloud.core.project}
+    teamname: YOURTEAM # kpt-set: ${teamname}
 spec:
   resources:
     requests:
@@ -38,8 +42,10 @@ metadata:
   labels:
     app: wordpress
   annotations:
-    projectId: 'PROJECT_ID' # kpt-set: ${gcloud.core.project}
-    teamname: 'YOURTEAM' # kpt-set: ${teamname}
+    abc: def
+    foo: bar
+    projectId: PROJECT_ID # kpt-set: ${gcloud.core.project}
+    teamname: YOURTEAM # kpt-set: ${teamname}
 spec:
   selector:
     matchLabels:
@@ -50,6 +56,9 @@ spec:
       labels:
         app: wordpress
         tier: mysql
+      annotations:
+        abc: def
+        foo: bar
     spec:
       containers:
       - name: mysql
diff --git a/wordpress-deployment.yaml b/wordpress-deployment.yaml
index a0a602a..af87ac8 100644
--- a/wordpress-deployment.yaml
+++ b/wordpress-deployment.yaml
@@ -5,8 +5,9 @@ metadata:
   labels:
     app: wordpress
   annotations:
-    projectId: 'PROJECT_ID' # kpt-set: ${gcloud.core.project}
-    teamname: 'YOURTEAM' # kpt-set: ${teamname}
+    abc: def
+    projectId: PROJECT_ID # kpt-set: ${gcloud.core.project}
+    teamname: YOURTEAM # kpt-set: ${teamname}
 spec:
   type: LoadBalancer
   selector:
@@ -22,8 +23,9 @@ metadata:
   labels:
     app: wordpress
   annotations:
-    projectId: 'PROJECT_ID' # kpt-set: ${gcloud.core.project}
-    teamname: 'YOURTEAM' # kpt-set: ${teamname}
+    abc: def
+    projectId: PROJECT_ID # kpt-set: ${gcloud.core.project}
+    teamname: YOURTEAM # kpt-set: ${teamname}
 spec:
   resources:
     requests:
@@ -38,8 +40,9 @@ metadata:
   labels:
     app: wordpress
   annotations:
-    projectId: 'PROJECT_ID' # kpt-set: ${gcloud.core.project}
-    teamname: 'YOURTEAM' # kpt-set: ${teamname}
+    abc: def
+    projectId: PROJECT_ID # kpt-set: ${gcloud.core.project}
+    teamname: YOURTEAM # kpt-set: ${teamname}
 spec:
   selector:
     matchLabels:
@@ -50,6 +53,8 @@ spec:
       labels:
         app: wordpress
         tier: frontend
+      annotations:
+        abc: def
     spec:
       containers:
       - name: wordpress