apiVersion: kpt.dev/v1alpha2
kind: Kptfile
metadata:
  name: app
pipeline:
  validators:
  - image: gcr.io/kpt-fn/set-label:unstable # mutator function is being used as validator
    configMap:
      tier: backend
