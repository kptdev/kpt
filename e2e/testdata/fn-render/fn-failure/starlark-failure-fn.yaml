#

apiVersion: fn.kpt.dev/v1alpha1
kind: StarlarkRun
metadata:
  name: httpbin-gen
source: "# AND conditional has syntax error forcing fn to fail\ndef isHTTPBin(r):\n
  \ return r[\"apiVersion\"] == \"apps/v1\" and r[\"kind\"] == \"Deployment\" and
  \n  r[\"metadata\"][\"name\"] == \"httpbin\"\n\n# filter out the httpbin deployment\nctx.resource_list[\"items\"]
  = [r for r in ctx.resource_list[\"items\"] if not isHTTPBin(r)]\n"
