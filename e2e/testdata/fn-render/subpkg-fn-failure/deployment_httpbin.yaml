
apiVersion: apps/v1
kind: Deployment
metadata:
  name: httpbin
  namespace: staging
  labels:
    app: backend
    tier: db
spec:
  replicas: 4
  selector:
    matchLabels:
      app: backend
      tier: db
  template:
    metadata:
      labels:
        app: backend
        tier: db
    spec:
      containers:
      - name: httpbin
        image: kennethreitz/httpbin
