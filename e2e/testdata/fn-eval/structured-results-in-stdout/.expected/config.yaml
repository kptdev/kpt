# This test case tests the structured output of the `eval` command.
# non-zero exit code and no changes in the resources.
testType: eval # One of the functions in the pipeline fails resulting in
# This test checks function results and result output is not idempotent
# because the results produced by search replace function gets changed
# in the subsequent runs. So skipping it for now.
nonIdempotent: true
image: gcr.io/kpt-fn/search-replace:v0.1
fnConfig: ../search-replace-conf.yaml
# Non-error messages should be printed to stdout
stdOut: "[INFO]"
