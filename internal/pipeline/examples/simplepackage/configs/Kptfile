apiVersion: kpt.dev/v1alpha1
kind: Kptfile
metadata:
  name: kptfile
pipeline:
  mutators:
  - image: gcr.io/kpt-fn/set-namespace
    configMap:
      namespace: staging
  - image: gcr.io/kpt-fn/set-label
    configMap:
      tier: db
