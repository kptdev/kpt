apiVersion: kpt.dev/v1alpha2
kind: Kptfile
metadata:
  name: app
pipeline:
  mutators:
  - image: gcr.io/kpt-fn/set-namespace
    configMap:
      namespace: staging
  - image: gcr.io/kpt-fn/set-label
    configMap:
      tier: backend
