apiVersion: apps/v1
kind: Deployment
metadata:
  namespace: PROJECT_ID-yourspace # {"$kpt-set":"proj-namespace"}
  name: query
spec:
  replicas: 1
  template:
    spec:
      containers:
      - name: query
        image: query:1.8.0 # {"$kpt-set":"image-tag"}
